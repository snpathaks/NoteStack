services:
  notes-api:
    build:
      context: ./app
      dockerfile: Dockerfile
    container_name: notes-api
    ports:
      - "3000:3000" # host:container
    environment:
      - PORT=3000
      - NODE_ENV=production
    restart: unless-stopped
